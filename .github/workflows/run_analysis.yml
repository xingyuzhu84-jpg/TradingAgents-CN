name: 自动运行分析

on:
  push:
    branches:
      - main

jobs:
  analyze:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          
      - name: Install dependencies
        run: |
          pip install -e .
          
      - name: Run analysis
        env:
          DASHSCOPE_API_KEY: sk-sp-ae6f1b9f26654904a759ff3554d799d0
          MONGODB_ENABLED: 'false'
          REDIS_ENABLED: 'false'
        run: |
          python -m cli.main analyze 600938 --stock_date 2026-03-06
